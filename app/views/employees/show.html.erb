<h1>Employee Show page</h1>
<p>Employee name:<%=@employee.name %></p>
<p>Employee Dept:<%=@employee.department.name %></p>
  <% @employee.tickets.oldie_youngie. map do|ticket|  %>
    <p>ticket age ordered:<%=ticket.age  %></p>

    <% end %>
  <% @employee.tickets.each do |ticket| %>
  <p>Employee ticket:<%=ticket.subject %></p>
  <p>Admission age:<%=ticket.age %></p>

  <%end %>

  ###working on it ran out of time!
  <%= button_to "Add tickect to #{employee.name}",employee_ticket_path(@employee.id, ticket.id)
  ###working on it ran out of time!
